XML格式：标签化传递信息的格式
JSON格式：采用键值对的格式。为JavaScript语言的子集。用于程序间的数据交换
YAML格式：同样采用键值对的格式，但是更多的应用在时间和日期这样的数据。但是和JSON不同，并非字典的形式。靠空格区分
配置文件：将一些参数保存下来。持久化。通常文件后缀为.xml或者.ini
DLL格式:动态链接库或者称之为应用程序扩展。好处是实现了模块化，只有当主程序用到该模块时才会调用该dll